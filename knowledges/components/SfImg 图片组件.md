# SfImg 图片组件

## 简介

基于 Element Plus 的 `ElImage` 组件封装。

## 基础用法

```html
<SfImg src="https://example.com/image.png" fit="cover" />
```

## API 参考

### Props (属性)

继承所有 `ElImage` 的属性。

### Slots (插槽)

继承所有 `ElImage` 的插槽。

### Exposed (暴露)

透传 `ElImage` 的所有方法和属性。
