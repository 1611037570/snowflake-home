1、图标数据来源是iconify图标库

2、使用 SfIcon 组件 已经全局挂载可以直接使用，传入icon名称即可
传入 SfIcon 组件的 `size` 是 **Tailwind 间距单位（即 设计稿 px 值 ÷ 4）**。例如设计稿图标大小为 20px 时，必须设置 `size="5"`，禁止直接传入 `"20"`。
<SFIcon icon="iconify:icon-name" size="5" />

3、apps\web\src\configs\modules\icon.ts 中定义了图标常量表，优先使用常量表中的图标名称，新增图标时必须在常量表中声明
