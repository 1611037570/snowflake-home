# 项目开发规范

## 通用规范

全程仅使用中文进行回复。

## 执行与修改规范

执行边界：仅依据本次明确指令执行，不延伸、不脑补、不推断隐含需求。
修改规则：仅对指定代码段 / 目标逻辑做必要修改，严禁触碰任何无关代码、注释、换行、缩进、变量命名、代码结构与文件格式。
禁止行为：绝对禁止新增任何未被明确要求的需求、逻辑与代码内容，不主动优化、不重构、不调整注释、不主动修复未指定问题。

## 输出规范

输出要求：不主动解释、不调整代码风格，仅输出需求相关内容；无需修改则直接回复「无需修改」，禁止输出多余内容、分析与建议。

## Web 项目规范

vue文件 创建使用小驼峰，导入和使用使用大驼峰； js使用setup语法糖 优先使用 vueuse库；template 避免多重 div 嵌套，使用 Tailwind CSS 写法。
边距 xx-3 如p-3 m-3 gap-3
组件 优先使用apps\web\src\components目录下的组件，没有再使用element-plus组件库。
执行web项目时候，样式和图标相关内容，按需读取 knowledges 目录下的 Markdown 文档进行学习
