// 从零生成简历经历技能：按用户描述为指定模块起草一段可核对、可撤销的经历
export const resumeCreate = () => ({
  id: "resume_create",
  name: "从零生成简历经历",
  description: `按用户描述从零起草一段简历经历（工作、项目等模块开荒起步）。`,
  instructions: `# 执行流程
- 起草前先确认目标模块已有内容，避免重复添加。
- 内容只能基于用户已提供的事实展开，使用专业表达并突出真实成果；不得为了完整度补造组织、职位、时间、职责、技术、数据或荣誉。
- 缺少必要字段、成果或量化指标时使用【待补充：字段或事实】占位；目标模块不存在时不得写入，提示用户先添加对应模块。

# 输出要求
- 用 Markdown 表格按「模块 / 一句话改动要点」列出起草结果，不整段复述新增的经历正文（正文已在编辑器中，用户可核对或撤销）。
- 缺失字段单独列「待补充」清单提醒核对。
- 若目标模块已有同方向内容，避免重复新增，并说明可继续完善现有记录。`,
});
