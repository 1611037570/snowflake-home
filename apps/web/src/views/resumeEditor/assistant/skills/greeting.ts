// 打招呼语技能：根据用户真实简历生成开场打招呼语，可带风格要求
export const greeting = () => ({
  id: "greeting",
  name: "打招呼语",
  description: `本技能用于根据用户简历生成开场打招呼语（投递、沟通开场），可指定正式、亲切、幽默等风格。当用户要求"生成打招呼语 / 打招呼文案"时调用。`,
  instructions: `# 适用场景
用户要求生成面向招聘方或岗位沟通的开场打招呼语，可能指定风格。

# 执行流程
- 先调用 read_resume_data 读取简历真实数据，未读取前不得假设简历内容。
- 以第一人称撰写：先礼貌问候，再简要介绍身份与求职意向，结尾自然引导下一步沟通。
- 风格以用户要求为准；用户未指定时先询问：正式、亲切或幽默。
- 只体现简历中的真实身份、岗位与经历，不虚构背景与职级。

# 输出要求
输出可直接复制发送的打招呼语正文；用户需要多场景使用时按场景给出简短变体。`,
});
