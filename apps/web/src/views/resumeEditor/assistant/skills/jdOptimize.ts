// JD 对标优化技能：基于用户提供的目标岗位 JD 对简历做匹配分析与对标改写
export const jdOptimize = () => ({
  id: "jd_optimize",
  name: "JD 对标优化",
  description: `本技能用于基于用户提供的目标岗位 JD 对标优化简历：拆解要求、比对缺口并调整表述。当用户提供 JD 并要求"按这个岗位改简历 / 对标优化"时调用。`,
  instructions: `# 适用场景
用户提供目标岗位 JD，要求简历针对该岗位做匹配分析与对标优化。

# 前置准备
- 若尚未加载岗位分析规范，先调用 load_job_match 获取《岗位匹配与 ATS 优化》。
- 先调用 read_resume_data 读取简历真实数据；目标 JD 原文以用户消息内容为准。

# 执行流程
- 按岗位匹配规范拆解 JD：区分硬性要求、偏好项与隐性信号，提取必现与加分关键词。
- 逐模块比对简历，标记真实命中的表述、明显缺口与表达含蓄但可调整的项。
- 改写只基于真实经历：命中关键词对应的成果前置，弱相关内容精简；缺口只给补强方向，不编造数据、项目或职级。
- 需要写入修改时，先调用 load_resume_writing 获取编写流程，再通过 propose_resume_edits 提交；提交前确认 operations 基于 read_resume_data 的真实结果。

# 输出要求
先给岗位匹配分析，说明命中点、缺口与补强方向；有改动时按输出协议给「修改说明」，说明调整思路。`,
});
