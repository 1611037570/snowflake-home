// 简历优化任务技能：承接"优化我的简历"类请求，先明确方向再衔接方法论与写操作
export const resumeOptimizeTask = () => ({
  id: "resume_optimize_task",
  name: "简历优化任务",
  description: `本技能用于承接"优化我的简历"类整体优化任务：先明确优化方向（整体结构/语言表达/内容润色），再按技能内指引执行。当用户要求"优化简历 / 把简历改得更好"时先加载本技能。`,
  instructions: `# 适用场景
用户要求优化整份简历（如"优化我的简历 / 帮我改得更好"）时按本技能执行；局部单段改写直接按 load_resume_writing 流程处理，无需本技能。

# 执行流程
- 先调用 read_resume_data 读取简历真实数据，未读取前不得假设简历内容。
- 优化方向未明确时先询问用户：整体结构、语言表达、内容润色。
- 调用 load_resume_optimization 获取写作方法论，按其规则执行优化。
- 保持内容真实：不增删事实、不编造经历、数据或职级；用户未提供的细节不得虚构。
- 需要写入时调用 load_resume_writing 获取编写流程，再通过 propose_resume_edits 提交修改。

# 输出要求
说明本次优化方向与改动思路；有改动时按输出协议组织「修改说明」。`,
});
