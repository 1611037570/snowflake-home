// 旧路径转导出独立包的通用调度类型，保持业务工具引用不变。
export { ToolRegistry } from "@snowflake/ai";
export type {
  ChatMessage,
  ReactConfig,
  ReactTool,
  ThinkResult,
  ToolCall,
  Observation,
} from "@snowflake/ai";
